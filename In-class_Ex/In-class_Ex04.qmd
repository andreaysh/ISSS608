---
title: "In-class Exercise 04"
author: "Andrea Yeo"
date-modified: "last-modified"
execute:
  echo: true
  eval: true
  warning: false
  freeze: true
---

## 1. Getting started

### 1.1 Installing and loading of R packages

```{r}
pacman::p_load(haven, SmartEDA,tidyverse, tidymodels,ggridges, colorspace)
```

#### 1.2  Importing data

```{r}
exam <- read_csv("data/Exam_data.csv", show_col_types = FALSE)
```

### 1.3 Visualing the target

```{r}
ggplot(data = exam,
       aes(x = ENGLISH,
           y = CLASS)) +
  geom_boxplot()
```

```{r}
ggplot(data = exam, aes(x = ENGLISH, y = CLASS)) +
  geom_density_ridges(
    scale = 3,
    rel_min_height = 0.01,
    bandwidth = 3.4,
    fill = lighten("#7097BB", .3),
    color = "white"
  ) +
  scale_x_continuous(
    name = "English grades",
    expand = c(0, 0)
  ) +
  scale_y_discrete(
    name = NULL, 
    expand = expansion(add = c(0.2, 2.6))
  ) +
  theme_ridges()
```

### 1.4 Oneway ANOVA Test: ggbetweenstats() method


